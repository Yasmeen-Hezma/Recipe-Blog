<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="/img/main.png" width="500" height="400" class="d-block mx-lg-auto img-fluid" loading="lazy"
            alt="Cooking With Node.js">
    </div>

    <div class="col-12 col-lg-6">
        <h3 class="display-6 fw-bold mb-3">Huge selection of Egyption recipe ideas</h3>
        <p class="lead">
            Explore the Flavors of Egypt and Beyond: From Traditional Delights to Modern Inspirations.
        </p>

        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>

            <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>

        </div>

    </div>

</div>
<!-- catogeries -->
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">

    <%if (typeof categories !=='undefined' && categories.length>0){%>
        <%categories.forEach((category,index)=>{%>
            <a href="/categories/<%- category.name %>" class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="/img/<%- category.image %>" alt=<%-category.name%> loading="lazy">
                </div>
                <div class="pt-1 ">
                    <%-category.name%>
                </div>
            </a>
            <%})%>
                <%}%>
</div>
<!-- End of catogeries -->
<!-- latest -->
<section>
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Recipes</h2>
        <a href="/explore-latest" class="ms-auto">View More</a>
    </div>
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if (typeof food.latest !=='undefined' && food.latest.length>0 ) { %>
            <% food.latest.forEach((recipe, index)=> { %>
                <a href="/recipe/<%= recipe._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="/img/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
                <% }); %>
                    <% } %>
    </div>
</section>
<!-- End of latest -->
<!-- Traditonal -->
<section>
    <div class="d-flex mb-2 align-items-center">
        <h2>Traditional Recipes</h2>
        <a href="/categories/Traditional" class="ms-auto">View More</a>
    </div>
    <div class="row g-2 g-lg-3">
        <% if (typeof food.traditional !=='undefined' && food.traditional.length> 0 ) { %>
            <% food.traditional.forEach((recipe, index)=> { %>
                <a href="/recipe/<%= recipe._id %>"
                    class="col-lg-2 col-md-3 col-sm-4 col-6 text-center category__link--main flex-grow-1">
                    <div class="category__img--main category__img--large shadow">
                        <img src="/img/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
                <% }); %>
                    <% } %>
    </div>
</section>

<!-- End of Traditional -->
<!-- Modern -->
<section>
    <div class="d-flex mb-2 align-items-center">
        <h2>Modern Recipes</h2>
        <a href="/categories/Modern" class="ms-auto">View More</a>
    </div>
    <div class="row g-2 g-lg-3">
        <% if (typeof food.modern !=='undefined' && food.modern.length> 0 ) { %>
            <% food.modern.forEach((recipe, index)=> { %>
                <a href="/recipe/<%= recipe._id %>"
                    class="col-lg-2 col-md-3 col-sm-4 col-6 text-center category__link--main flex-grow-1">
                    <div class="category__img--main category__img--large shadow">
                        <img src="/img/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
                <% }); %>
                    <% } %>
    </div>
</section>
<!-- End of Modern -->
<!-- Drinks -->
<section>
    <div class="d-flex mb-2 align-items-center">
        <h2>Drinks</h2>
        <a href="/categories/Drinks" class="ms-auto">View More</a>
    </div>
    <div class="row g-2 g-lg-3">
        <% if (typeof food.drinks !=='undefined' && food.drinks.length> 0 ) { %>
            <% food.drinks.forEach((recipe, index)=> { %>
                <a href="/recipe/<%= recipe._id %>"
                    class="col-lg-2 col-md-3 col-sm-4 col-6 text-center category__link--main flex-grow-1">
                    <div class="category__img--main category__img--large shadow">
                        <img src="/img/<%= recipe.image %>" alt="<%= recipe.name %>" loading="lazy">
                    </div>
                    <div class="pt-1">
                        <%= recipe.name %>
                    </div>
                </a>
                <% }); %>
                    <% } %>
    </div>
</section>
<!-- End of Drinks -->
<!-- Submit -->
<section class="px-4 py-5 my-5 text-center">
    <img src="/img/Submit.jpg" alt="Publish your recipe for FREE " width="566" height="288" loading="lazy"
        class="d-block mx-auto mb-4 img-fluid">
    <h1 class="display-5 fw-bold"> Publish your recipe for FREE</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">From your kitchen to our blog, submit your recipe for FREE and let the cooking
            inspiration
            spread!</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center ">
            <a href="/submit-recipe" class="btn btn-primary btn-dark btn-lg"> Submit Recipe</a>
        </div>
    </div>
</section>
<!-- End of Submit -->
</div>